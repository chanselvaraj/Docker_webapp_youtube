# Docker_webapp_youtube
🐍 Flask + PostgreSQL Web Application using Docker  This project demonstrates how to build and containerize a simple Python Flask web application that connects to a PostgreSQL database using Docker and Docker Compose.
